---
footer.text: "Frontmatter Footer Wins"
---

# Footer Override

A tiny document whose frontmatter sets `footer.text`. The paired config
(`footer-override.yaml`) enables the footer with a different text; this
document's value must win in the effective config the renderer consumes.
